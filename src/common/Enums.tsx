export enum MissionStatus {
    Waiting,
    InProgress,
    Blocked,
    Done
}
export enum MissionPriority {
    Urgent,
    Highest,
    High,
    Normal,
    Low,
    Lowest
}
export enum MissionUpdateOperation{
    Add,
    Update,
    Delete
}